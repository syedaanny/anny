
import React from 'react';
import styles from '@/styles/Navbar.css';

const HomeSection = () => {
  return (
    <section className={styles.homeSection}>
      <div className={styles.flexContainer}>
        {/* Your content with flex display */}
      </div>
    </section>
  );
};

export default HomeSection;
